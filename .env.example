# PostgreSQL connection string (leave blank to use SQLite in dev)
DATABASE_URL=postgresql://user:password@host/dbname

# Admin key for the dashboard (required — no default in production)
ADMIN_KEY=your_secret_key_here
